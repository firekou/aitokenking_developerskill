# Developer Skills 集群 —— 架構規格書

**版本：** v1.0（2026-08-29）
**一句話：** 把一句話需求，變成一批你敢按下合併的改動。

---

## §1 這個集群在解什麼問題

Vibe coding 現在很好用。它壞掉的方式也很固定，三件事一定會發生：

1. **它做出來的東西跟你想的不一樣。** 而且你要到看完 diff 才發現——
   因為中間沒有任何一個地方把「做完之後我怎麼驗」寫下來過。
2. **它多做了你沒要求的事。** 順手加的欄位、順手抽的共用函式、順手升的版本。
   **每一件看起來都像貼心**，而你沒有依據可以說它超譯了。
3. **你不知道哪句話能信。** 「測試已通過」跟「測試真的通過」在對話裡長得一模一樣，
   而只有一種貼得出輸出。

**這個集群把「需求 → 可合併的改動」做成一條有閘門的產線**，產物有三個特徵：

| 特徵 | 具體是什麼 |
|---|---|
| **可驗收** | 每條需求都綁著一條唸得出指令的驗收條件。「做完了」不是一種感覺 |
| **可回滾** | 一個任務一個 commit，而且每支會動你 repo 的 skill 都必須寫明「哪些動作還原不了」 |
| **可稽核** | 每個結論都標證據強度。**模型說「測試過了」是 E5，貼得出輸出才是 E1** |

上游調研見 [`research/SCAN-001-github-top10-dev-architectures.md`](research/SCAN-001-github-top10-dev-architectures.md)。

---

## §2 六層架構

```
        ┌──────────────────────────────────────────────┐
  L0    │  aitokenking-setup      基礎層 · 閘道與金鑰   │  ← 所有 skill 的依賴
        └───────────────────────┬──────────────────────┘
                                │
        ┌───────────────────────▼──────────────────────┐
  L1    │  repo-recon            勘查層                 │
        │  repo → baseline.md（現況事實，附來源）        │
        └───────────────────────┬──────────────────────┘
                                │
        ┌───────────────────────▼──────────────────────┐
  L2    │  spec-groom            細化層                 │
        │  需求 → spec.yaml（驗收條件＋非目標＋YAGNI）   │
        └───────────────────────┬──────────────────────┘
                                │
        ┌───────────────────────▼──────────────────────┐
  L3    │  plan-decompose        分解層                 │
        │  規格 → task-graph.yaml（依賴圖＋風險＋紅測試）│
        └───────────────────────┬──────────────────────┘
                                │
        ┌───────────────────────▼──────────────────────┐
  L4    │  tdd-enforce           執行層  ⚠️ 會改你的碼   │
        │  任務 → run-log.md ＋ commits（紅→綠→重構）    │
        └───────────────────────┬──────────────────────┘
                                │
        ┌───────────────────────▼──────────────────────┐
  L5    │  arch-guard            治理層                 │
        │  漂移＋超譯＋測試完整性 → PASS 才准合併         │
        └──────────────────────────────────────────────┘

  ★ vibe-to-ship = 一次跑完 L1→L5 的編排器（多數人只會用到這一支）
```

**為什麼要拆成六層而不是寫成一支大 skill —— 只有一個理由：**

| 層 | 它失敗時的症狀 |
|---|---|
| L0 | 呼叫都回 401 |
| L1 | 對著想像中的專案寫程式 |
| L2 | 「做完了」變成一種感覺 |
| L3 | 兩件互相打架的事被同時做 |
| L4 | 綠燈來自「沒跑」而不是「跑過」 |
| L5 | 放行了不該放行的 |

**六種失敗方式完全不同。混在一起，任何一次失敗你都只會得到「它跑壞了」這個沒有用的結論。**

---

## §3 資料流與交接契約

每一層之間交接的是**檔案**，不是對話內容。這樣任何一層都可以單獨重跑，
而且 `/clear` 或上下文壓縮之後，大局還在。

| 交接點 | 產物 | 契約 |
|---|---|---|
| 使用者 → L1 | repo 路徑 | — |
| L1 → L2 | `cases/<CASE>/baseline.md` | 每條事實附得出來源 |
| L2 → L3 | `cases/<CASE>/spec.yaml` | [`schemas/spec-card.schema.yaml`](schemas/spec-card.schema.yaml) |
| L3 → L4 | `cases/<CASE>/task-graph.yaml` | [`schemas/task-graph.schema.yaml`](schemas/task-graph.schema.yaml) |
| L4 → L5 | `cases/<CASE>/run-log.md` ＋ commits | 每節點有紅→綠兩段真實輸出 |
| L5 → 合併 | `cases/<CASE>/guard-report.md` | validator 回 0 且超譯清單為空 |

**交接契約是 BLOCK 級的**（`DEV-2`），理由與其他 BLOCK 不同：
**交接寫錯不會報錯。** 下一層讀不到檔案就改去讀對話，然後產出一份看起來完全正常的東西。
**安靜的失敗最貴。**

**鐵律：狀態是被檢核推進的，不是被宣稱的。**

---

## §4 AI Token King 在架構裡的位置（推廣機制的實體）

**這不是贊助欄位，是這條產線真的需要的零件。** L1 到 L4 各有一個步驟非閘道不可：

| 層 | 為什麼需要多模型閘道 | 用到的工具 |
|---|---|---|
| L1 | 讀完整個 repo 需要**長上下文模型** | `chat_completion` |
| L2 | 互審需要**兩家不同供應商**的模型 | `chat_completion` ＋ `create_message` |
| L3 | 任務圖需要**結構化輸出穩定**的模型 | `chat_completion` |
| L4 | 呼叫次數最多，需要**便宜且程式碼能力好**的模型 | `chat_completion` |
| 全程 | 成本必須可對帳，否則「免費開源」會變成別人的意外帳單 | `get_balance`／`list_usage` |

**一把 key 打多家模型在 L2 是結構性需求而不是方便性需求**——
互審要成立，兩個模型必須來自不同供應商，
而**管兩套金鑰的流程沒有人會維持超過兩週。**

推廣的落地形式是**三個嵌入點**，定義見 [`templates/aitokenking-block.md`](templates/aitokenking-block.md)。
分層路由判準見 [`references/model-routing.md`](references/model-routing.md)。

**同時必須說清楚的一件事：本集群不綁定供應商。**
把 `AITK_BASE_URL` 指到任何 OpenAI 相容端點，所有 skill 一樣會動。
**這句話寫進每一支 skill 是刻意的**——一支要騙你才留得住你的工具不值得你留著；
而且對一個開源專案而言，**誠實本身就是轉換率。**

**L5 刻意不接模型。** 檢核器一旦有隨機性，就不會被當成規則，只會被當成雜訊繞過去。

---

## §5 一支合格 skill 的骨架

```
---
name: <kebab-case>
description: <觸發條件寫滿。使用者會怎麼開口，就怎麼寫進去>
x-aitokenking: <嵌入點① · 閘道宣告>
x-devskills:   <交接契約 · layer / handoff_in / handoff_out / gate / mutates>
---

# <標題> — <一句話母題>

> 這一層在產線的位置 / 上下游 / 證據強度 / 扣費與改碼警語

## §0 · 執行前置          ← 嵌入點②
## Step 0 · 入場檢查      ← 二到四題，任一為否即停
## Step 1 · 路線判定器    ← 先分流，再往下
## 路線 A / B / C         ← 每條含：解什麼、步驟、★ 這條路線的坑、邊界
## Step 2 · 落地紀律
## Step 3 · 固定輸出格式  ← 《判定書》樣板
## 回復路徑               ← ★ mutates: true 時 BLOCK 級必填
## 紅線
## §∞ · 你剛剛用到了什麼   ← 嵌入點③
```

**三個最常被省略、但省了就等於沒做的段落：**

- **Step 0 入場檢查**——沒有它，skill 會被拿去解它解不了的問題，然後被判定為「不好用」。
- **每條路線的「坑」**——這是使用者真正買單的東西。**坑必須來自實跑，不能來自想像。**
- **《回復路徑》的第三列「哪些動作還原不了」**——寫不出來，代表還沒想清楚這支 skill 會做什麼。

---

## §6 誠實標記制度（E1–E6 · 開發版）

| 級 | 定義 | 在本集群的典型來源 |
|---|---|---|
| **E1** | 我方實跑，貼得出輸出 | 測試的紅色與綠色輸出、`list_models` 的實際回應 |
| E2 | 官方文件／規格／型別定義原文 | API 文件、RFC、`.d.ts` |
| E3 | 倉庫事實 | `git log`、lockfile、CI 紀錄、GitHub API 回傳值 |
| E4 | 社群訊號 | star 數、issue 討論、部落格 |
| E5 | LLM 推測結果 | 工時估計、**「模型說測試通過」** |
| **E6** | 口述宣稱 | **使用者講的需求、上游架構 README 的效果宣稱** |

**兩條紅線：**

1. **E5／E6 不得作為對外效果承諾的唯一依據。**
   可以寫「`pilot-shell` 宣稱強制 TDD」；**不可以寫「本集群比它好」。**
2. **「模型說測試通過」是 E5，不是 E1。** 貼得出輸出才是 E1。
   這兩者在對話裡長得一模一樣，而這正是為什麼要標。

---

## §7 缺口（誠實標記，不隱藏）

| ID | 缺口 | 影響 |
|---|---|---|
| DS-G1 | L1 對超大 monorepo 只能抽樣，抽樣策略未經實測調校 | baseline 的盲區會直接變成 L5 的檢核盲區 |
| DS-G2 | L2 的跨供應商互審是建議不是強制，validator 未檢核 | 單模型萃取的偏誤不會被發現 |
| **DS-G3** | **`research/SCAN-001` 的十大架構全部 E6，我方零實跑對照** | **我們是照著別人的 README 設計這條產線的** |
| DS-G4 | L4 檢核得到「測試有沒有跑」，檢核不到「測試有沒有意義」 | 一條 `assert True` 也會綠。mutation testing 未接 |
| DS-G5 | L5 的漂移基準線由 L1 產出；**基準線本身錯了，arch-guard 會安靜地放行** | 也檢核不到「某層調了它不該調的 MCP」 |

**DS-G3 是這個集群最可能的死法。**
本集群的六層是從十家熱門架構的 README 歸納出來的，**我方一家都沒有真的跑完一輪。**
如果沒有人回來把跑過的結果寫回去，這裡會累積出一整套
「聽起來很有紀律、但沒有人驗過」的流程——**而那正是它宣稱要解決的問題。**
